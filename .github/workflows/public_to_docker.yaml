name: Docker Build and Publish
on:
    push:
        branches:
            - main

jobs:
    build:
        name: Build and Publish Docker Image
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Set up Docker Image Version
              id: prep
              run: |
                  PR_TITLE="${{ github.event.pull_request.title }}"
                  SAFE_TAG=$(echo "$PR_TITLE" | tr "[:upper:]" "[:lower:]" | sed 's/[^a-z0-9-]/-/g' | cut -c 1-30)
                  echo "IMAGE_VERSION=$SAFE_TAG" >> $GITHUB_ENV

            - name: Login to the Dockerhub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: print the image version
              run: |
                  echo "${{ github.event.pull_request.title }}"

            # - name: Build and Push Docker Image
            #   run: |
            #       docker-compose -f ./docker-compose.publish.yaml build
            #       docker-compose -f ./docker-compose.publish.yaml push
